apiVersion: v1
kind: Service
metadata:
  name: homarr
  labels:
    app.kubernetes.io/name: homarr
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: homarr
  ports:
    - name: http
      port: 80
      targetPort: http
