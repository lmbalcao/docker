apiVersion: v1
kind: Service
metadata:
  name: tdarr
  labels:
    app.kubernetes.io/name: tdarr
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: tdarr
  ports:
    - name: http
      port: 80
      targetPort: http
