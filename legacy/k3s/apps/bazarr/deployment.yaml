apiVersion: apps/v1
kind: Deployment
metadata:
  name: bazarr
  labels:
    app.kubernetes.io/name: bazarr
    app.kubernetes.io/part-of: media
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: bazarr
  template:
    metadata:
      labels:
        app.kubernetes.io/name: bazarr
    spec:
      securityContext:
        runAsUser: 1007
        runAsGroup: 1027
        fsGroup: 1027
        fsGroupChangePolicy: OnRootMismatch
      initContainers:
        - name: init-nfs-bazarr
          image: busybox:1.36
          command: ["sh", "-c", "set -eux; mkdir -p /init-media/bazarr /init-downloads/bazarr"]
          securityContext:
            runAsUser: 1007
            runAsGroup: 1027
          volumeMounts:
            - name: media-rwx
              mountPath: /init-media
            - name: downloads-rwx
              mountPath: /init-downloads
      containers:
        - name: bazarr
          image: lscr.io/linuxserver/bazarr:latest
          imagePullPolicy: Always
          env:
            - name: PUID
              value: "1007"
            - name: PGID
              value: "1027"
            - name: TZ
              value: "Europe/Lisbon"
          ports:
            - containerPort: 6767
              name: http
          volumeMounts:
            - name: media-rwx
              mountPath: /media
              subPath: bazarr
            - name: downloads-rwx
              mountPath: /downloads
              subPath: bazarr
            - name: config
              mountPath: /config
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: "2"
              memory: 2Gi
      volumes:
        - name: media-rwx
          persistentVolumeClaim:
            claimName: media-rwx
        - name: downloads-rwx
          persistentVolumeClaim:
            claimName: downloads-rwx
        - name: config
          emptyDir: {}
