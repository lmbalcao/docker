apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-rwx
  namespace: immich
spec:
  storageClassName: ""         # <- impede herdar "local-path"
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  volumeName: pv-immich-rwx     # <- liga explicitamente ao PV
