# State

- Evidência: `README.md` descreve este repo como catálogo de stacks Docker/Compose e indica estado ativo.
- Evidência: existem múltiplas subpastas de stacks e workflow de agentes versionado.
- Estado atual: documentação base criada em `docs/`; runtime local deve ficar fora do Git.
